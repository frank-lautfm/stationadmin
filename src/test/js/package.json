{
  "name": "stationadmin-tests",
  "version": "1.0.0",
  "description": "Tests for StationAdmin shuffle algorithm",
  "type": "commonjs",
  "scripts": {
    "test": "node StationAdminTests.js",
    "build": "cd ../../main/javascript/shuffle && npx tsc && npx prettier --write StationAdmin.js && node -e \"const fs=require('fs'),f='StationAdmin.js',h=fs.readFileSync('StationAdmin.ts','utf8').split('\\n').slice(0,2).join('\\n')+'\\n\\n';fs.writeFileSync(f,h+fs.readFileSync(f,'utf8'));\"",
    "build-and-test": "npm run build && npm test"
  },
  "dependencies": {
    "alea": "^0.0.9"
  }
}
