{
  "name": "stationadmin",
  "version": "1.0.0",
  "description": "StationAdmin JavaScript build",
  "scripts": {
    "build": "cd src/main/javascript/shuffle && npx tsc && npx prettier --write StationAdmin.js && node -e \"const fs=require('fs'),f='StationAdmin.js',h=fs.readFileSync('StationAdmin.ts','utf8').split('\\n').slice(0,2).join('\\n')+'\\n\\n';fs.writeFileSync(f,h+fs.readFileSync(f,'utf8'));\"",
    "test": "cd src/test/js && node StationAdminTests.js",
    "build-and-test": "npm run build && npm test"
  }
}
